import React from 'react'
import { Slide, Heading } from 'spectacle'

export default (
    <Slide transition={['fade']} bgColor="primary">
      <Heading size={1} caps>
        HOCs, FACCs, render props, compound components
      </Heading>
    </Slide>
)
